version: '3.8'

services:
  m2proxy:
    build: .
    container_name: m2proxy
    ports:
      - "1234:1234"
    restart: unless-stopped
    environment:
      - RUST_LOG=info
    command: ["--host", "0.0.0.0", "--port", "1234"]

